version: 2
jobs:
  apply-live:
    docker:
      - image: ilyasotkov/exekube:latest
    steps:
      - checkout
      - run:
          name: Test if CircleCI contexts work
          command: echo $TF_VAR_cloudflare_auth
  deploy:
    docker:
      - image: squidfunk/mkdocs-material:latest
    steps:
      - checkout
      - run: echo $CIRCLE_REPOSITORY_URL
      - run:
          name: Build and deploy website to GitHub Pages
          command: mkdocs gh-deploy --message "[ci skip] Deployed with MkDocs"
workflows:
  version: 2
  deploy:
    jobs:
      - deploy
      - apply-live:
          context: org-global
