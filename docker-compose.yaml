version: "3.5"

services:
  exekube:
    build:
      context: .
      dockerfile: ./build/Dockerfile
    volumes:
      - .:/exekube
      - ./config/gcloud:/root/.config/gcloud
      - ./config/kube:/root/.kube
    working_dir: /exekube
